{
  "variables": {
    "template": "centos-7",
    "guest_os_type_vmware": "centos-64",
    "guest_os_type_virtualbox": "RedHat_64",

    "debug": "false",
    "headless": "false",

    "iso_url": "/Users/dan/stuff/iso/os/centos/CentOS-7-x86_64-NetInstall-1810.iso",
    "iso_checksum": "19d94274ef856c4dfcacb2e7cfe4be73e442a71dd65cc3fb6e46db826040b56e",
    "iso_checksum_type": "sha256",

    "installation_source": "http://mirror.centos.org/centos/7/os/x86_64/",
    "installer_boot_time": "5s",

    "vmware_hardware_version": "9",
    "memory_size": "1024",
    "cpu_count": "1",
    "disk_size_vmware": "10240",
    "disk_size_virtualbox": "10240",
    "vmtools_iso_path": "/tmp/vmware-tools.iso",
    "guest_additions_path": "/tmp/VBoxGuestAdditions.iso",

    "communicator": "ssh",
    "ssh_username": "packer",
    "ssh_private_key": "../../common/keys/vagrant",
    "ssh_timeout": "15m",

    "shutdown_command": "sudo /sbin/poweroff",

    "admin_create": "true",
    "admin_user": "vagrant",
    "admin_group": "vagrant",
    "admin_passwd": "vagrant",
    "admin_uid": "1000",
    "admin_gid": "1000",
    "admin_groups": "wheel",
    "admin_shell": "/bin/bash",
    "admin_gecos": "CentOS Administrator",
    "admin_ssh_authorised_key": "ssh-rsa  vagrant insecure public key",

    "selinux_relabel": "false",

    "packer_dir": "/packer",
    "packer_virt_sysprep_dir": "/packer-virt-sysprep",
    "zero_script_upload_path": "/tmp/zero-free-space.sh",

    "local_shell_binary": "/opt/local/bin/bash",
    "vagrantfile_base_dir": "vagrantfiles",
    "vagrantfile_filename_prefix": "Vagrantfile",
    "vagrant_cloud_org": "foosite"
  },

  "builders": [
    {
      "type": "vmware-iso",
      "http_directory": "../common/http",
      "communicator": "{{user `communicator`}}",
      "output_directory": "output-{{user `template`}}-{{build_name}}",
      "headless": "{{user `headless`}}",

      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",

      "boot_command": [
        "<tab> <wait>",
        "inst.text inst.repo={{user `installation_source`}} inst.ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks.cfg inst.nosave=all<wait>",
        "<enter> <wait>"
      ],
      "boot_wait": "{{user `installer_boot_time`}}",

      "version": "{{user `vmware_hardware_version`}}",
      "guest_os_type": "{{user `guest_os_type_vmware`}}",
      "vmx_data": {
        "cpuid.coresPerSocket": "1",
        "mks.enable3d": "FALSE",
        "floppy0.present": "FALSE",
        "usb.vbluetooth.startConnected": "FALSE"
      },
      "vm_name": "{{user `template`}}-{{build_name}}",
      "display_name": "Packer-{{user `template`}}-{{build_name}}",
      "cpus": "{{user `cpu_count`}}",
      "memory": "{{user `memory_size`}}",
      "vmdk_name": "{{user `template`}}-{{build_name}}",
      "disk_type_id": "0",
      "disk_size": "{{user `disk_size_vmware`}}",
      "network_adapter_type": "vmxnet3",
      "sound": false,
      "serial": "NONE",
      "usb": false,
      "tools_upload_flavor": "linux",
      "tools_upload_path": "{{user `vmtools_iso_path`}}",

      "ssh_username": "{{user `ssh_username`}}",
      "ssh_private_key_file": "{{user `ssh_private_key`}}",
      "ssh_timeout": "{{user `ssh_timeout`}}",

      "shutdown_command": "{{user `shutdown_command`}}"
    },
    {
      "type": "virtualbox-iso",
      "http_directory": "../common/http",
      "communicator": "{{user `communicator`}}",
      "output_directory": "output-{{user `template`}}-{{build_name}}",
      "headless": "{{user `headless`}}",

      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",

      "boot_command": [
        "<tab> <wait>",
        "inst.text inst.repo={{user `installation_source`}} inst.ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks.cfg inst.nosave=all<wait>",
        "<enter> <wait>"
      ],
      "boot_wait": "{{user `installer_boot_time`}}",

      "guest_os_type": "{{user `guest_os_type_virtualbox`}}",
      "vboxmanage": [
        ["modifyvm", "{{.Name}}", "--vram", "16"],
        ["setextradata", "global", "GUI/SuppressMessages", "all"]
      ],
       "export_opts": [
        "--ovf20"
      ],
      "vm_name": "{{user `template`}}-{{build_name}}",
      "cpus": "{{user `cpu_count`}}",
      "memory": "{{user `memory_size`}}",
      "hard_drive_interface": "sata",
      "disk_size": "{{user `disk_size_virtualbox`}}",
      "sound": "none",
      "usb": false,

      "guest_additions_mode": "upload",
      "guest_additions_path": "{{user `guest_additions_path`}}",

      "ssh_username": "{{user `ssh_username`}}",
      "ssh_private_key_file": "{{user `ssh_private_key`}}",
      "ssh_timeout": "{{user `ssh_timeout`}}",

      "shutdown_command": "{{user `shutdown_command`}}"
    }
  ],

  "provisioners": [
    {
      "type": "shell",
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "environment_vars": [
        "PACKER_DIR={{user `packer_dir`}}",
        "PACKER_VIRT_SYSPREP_DIR={{user `packer_virt_sysprep_dir`}}"
      ],
      "inline": [
        "mkdir --mode=777 $PACKER_DIR",
        "mkdir --mode=777 $PACKER_VIRT_SYSPREP_DIR"
      ]
    },
    {
      "type": "file",
      "source": "../common/scripts/packer-virt-sysprep/",
      "destination": "{{user `packer_virt_sysprep_dir`}}"
    },
    {
      "type": "shell",
      "remote_folder": "{{user `packer_dir`}}",
      "expect_disconnect": true,
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "ADMIN_CREATE={{user `admin_create`}}",
        "ADMIN_USER={{user `admin_user`}}",
        "ADMIN_GROUP={{user `admin_group`}}",
        "ADMIN_PASSWD={{user `admin_passwd`}}",
        "ADMIN_UID={{user `admin_uid`}}",
        "ADMIN_GID={{user `admin_gid`}}",
        "ADMIN_GROUPS={{user `admin_groups`}}",
        "ADMIN_SHELL={{user `admin_shell`}}",
        "ADMIN_GECOS={{user `admin_gecos`}}",
        "ADMIN_SSH_AUTHORISED_KEY={{user `admin_ssh_authorised_key`}}",
        "SELINUX_RELABEL={{user `selinux_relabel`}}"
      ],
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "scripts": [
        "../common/scripts/00-remove-extraneous-packages.sh",
        "../common/scripts/01-yum-update.sh",
        "../common/scripts/02-remove-old-kernels.sh",
        "../common/scripts/03-disable-system-beep.sh",
        "../common/scripts/04-configure-ssh.sh",
        "../common/scripts/05-centos-systemd-fixes.sh",
        "../common/scripts/06-configure-chrony.sh",
        "../common/scripts/07-configure-timedate-settings.sh",
        "../common/scripts/08-create-admin-user.sh",
        "../common/scripts/09-selinux.sh"
      ]
    },
    {
      "type": "shell",
      "only": ["vmware-iso"],
      "remote_folder": "{{user `packer_dir`}}",
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "VMTOOLS_ISO_PATH={{user `vmtools_iso_path`}}"
      ],
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "scripts": [
            "../common/scripts/20-install-open-vm-tools.sh",
            "../common/scripts/21-install-vmware-tools.sh"
      ]
    },
    {
      "type": "shell",
      "only": ["virtualbox-iso"],
      "remote_folder": "{{user `packer_dir`}}",
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "GUEST_ADDITIONS_PATH={{user `guest_additions_path`}}"
      ],
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "scripts": [
            "../common/scripts/20-install-vbox-guest-additions.sh"
      ]
    },
    {
      "type": "shell",
      "remote_folder": "{{user `packer_dir`}}",
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "PACKER_VIRT_SYSPREP_DIR={{user `packer_virt_sysprep_dir`}}"
      ],
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "scripts": [
        "../common/scripts/80-create-packer-virt-sysprep-unit-file.sh",
        "../common/scripts/81-create-packer-virt-sysprep-run-control-script.sh",
        "../common/scripts/82-create-remove-packer-user-unit-file.sh"
      ]
    },
    {
      "type": "shell",
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "environment_vars": [
        "PACKER_DIR={{user `packer_dir`}}"
      ],
      "inline": [
        "rm -rf $PACKER_DIR"
      ]
    },
    {
      "type": "file",
      "source": "../common/scripts/zero-free-space.sh",
      "destination": "{{user `zero_script_upload_path`}}"
    },
    {
      "type": "shell",
      "environment_vars": [
        "ZERO_SCRIPT_UPLOAD_PATH={{user `zero_script_upload_path`}}"
      ],
      "execute_command": "sudo {{ .Vars }} $(command -v bash) '{{.Path }}'",
      "scripts": [
        "../common/scripts/90-create-zero-free-space-unit-file.sh"
      ]
    }
  ],

  "post-processors": [
    {
      "type": "shell-local",
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "BOX_USERNAME={{user `admin_user`}}",
        "BOX_USERPASSWD={{user `admin_passwd`}}",
        "COMMUNICATOR={{user `communicator`}}",
        "TEMPLATE={{user `template`}}",
        "VAGRANTFILE_BASE_DIR={{user `vagrantfile_base_dir`}}",
        "VAGRANTFILE_NAME={{user `vagrantfile_filename_prefix`}}.{{build_type}}",
        "VAGRANT_CLOUD_ORG={{user `vagrant_cloud_org`}}"
      ],
      "execute_command": [
        "{{user `local_shell_binary`}}", "-c", "chmod +x {{.Script}} && {{.Vars}} {{.Script}}; chmod -x {{.Script}}"
      ],
      "scripts": [
        "../../common/scripts/post-processor/00-generate-box-vagrantfiles.sh"
      ]
    },
    {
      "type": "vagrant",
      "keep_input_artifact": true,
      "vagrantfile_template": "{{user `vagrantfile_base_dir`}}/{{user `template`}}/{{user `vagrantfile_filename_prefix`}}.{{build_type}}",
      "vagrantfile_template_generated": true,
      "output": "boxes/{{user `template`}}-{{.Provider}}.box"
    },
    {
      "type": "shell-local",
      "environment_vars": [
        "DEBUG={{user `debug`}}",
        "VAGRANTFILE_BASE_DIR={{user `vagrantfile_base_dir`}}"
      ],
      "execute_command": [
        "{{user `local_shell_binary`}}", "-c", "chmod +x {{.Script}} && {{.Vars}} {{.Script}}; chmod -x {{.Script}}"
      ],
      "scripts": [
        "../../common/scripts/post-processor/99-clean-up-generated-box-vagrantfiles.sh"
      ]
    }
  ]
}
